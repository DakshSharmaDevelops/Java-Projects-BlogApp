services:
  - type: web
    name: blog-app
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: dbHost
        fromDatabase:
          name: blog-db
          property: host
      - key: dbPort
        fromDatabase:
          name: blog-db
          property: port
      - key: dbName
        fromDatabase:
          name: blog-db
          property: database
      - key: dbUsername
        fromDatabase:
          name: blog-db
          property: user
      - key: dbPassword
        fromDatabase:
          name: blog-db
          property: password
      - key: GROQ_API_KEY
        sync: false

databases:
  - name: blog-db
    databaseName: blogdb
    user: bloguser
    plan: free
